<!DOCTYPE html>
<html lang="en">
<head>
  <% include partials/_header %>
  <title>TinyApp</title>
</head>
<body>
    <% for (url in urls) { %>
        <li><%= url %> - <%= urls[url]%></li>
    <% }; %>
</body>
</html>